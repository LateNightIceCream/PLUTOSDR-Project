
addpath(genpath("../../transmitter/"))

SP = PR_QAM_Tx_Init

disp("Transmitting offset calibrationsignal")
% send the signal for frequency offset calibration
[SP, tx] = frequency_offset_transmit(SP, 80e3);

% send the message
runPR_QAM_Tx_TEST(SP, tx);


release(tx)